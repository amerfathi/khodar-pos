# 🧭 Brraka POS — Mobile Duplicate Route & Component Report
**المستند**: تقرير رصد وحسم المسارات والمكونات المزدوجة والمتنافسة  
**النظام**: منظومة براكه المحاسبية لإدارة المتاجر والكاشير (`Brraka POS`)  
**الإصدار**: `v2.6.0`  
**تاريخ التوثيق**: ٢٠ سبتمبر ٢٠٢٦  

---

## 1. جدول تقييم وحسم المكونات والمسارات المزدوجة

| المكون / المسار | الحالة السابقة | القرار المتخذ | السبب والتعليل البرمجي والتشغيلي (Rationale) |
|---|---|:---:|---|
| **`SettingsModal.jsx`** | نافذة إعدادات منبثقة مختصرة تفتح عند النقر على أيقونة الترس | **REMOVE / REDIRECT** | تلغى تماماً. تسبب ارتباكاً للمستخدم لكونها تفتقر لإدارة المستخدمين، الطابعات، والضرائب. يتم توجيه كل نقرات الترس إلى الشاشة القياسية `SettingsView`. |
| **`SettingsView.jsx`** | صفحة إعدادات شاملة وكاملة من 2000 سطر بـ 6 تبويبات | **KEEP (Canonical Settings)** | هي الوجهة القياسية الوحيدة لكافة إعدادات وضبط النظام (بيانات المتجر، الفروع، الطابعات، الضرائب، إدارة المستخدمين والصلاحيات، النسخ الاحتياطي، والمزامنة السحابية). |
| **`LoginView.jsx`** | شاشة تسجيل الدخول للهواتف مع تبويبين (دخول + تحميل تطبيقات) | **MERGE (Canonical Mobile Login)** | إزالة تبويب "تحميل التطبيقات" غير المنطقي في تطبيق مثبت على الهاتف، وجعل الشاشة نقية تماماً وسريعة وتركز بنسبة 100% على الدخول واستعادة الحساب. |
| **`DesktopLoginView.jsx`** | شاشة دخول خاصة ببيئة سطح المكتب Electron مع أزرار الإطار | **KEEP (Desktop Only)** | تبقى حصرية لبيئة الويندوز المكتبي للتعامل مع إطار النافذة المخصص بدون تشويش على بيئة الجوال. |
| **`BottomNav.jsx`** | شريط تنقل سفلي بـ 4 أزرار ثابته تُفقد حالتها عند الدخول لأقسام أخرى | **MERGE & ENHANCE (Canonical Nav)** | إعادة هيكلة الأزرار لتشمل أهم الإجراءات اليومية للكاشير مع تبويب "المزيد / القائمة" للتنقل السلس والسريع. |
| **`MobileHomeHub.jsx`** | لوحة تحكم شبكية للجوال | **KEEP (Canonical Mobile Dashboard)** | الواجهة الرئيسية النموذجية للوصول السريع لجميع الأقسام والوظائف. |
| **`DesktopSidebar.jsx`** | قائمة جانبية عمودية | **KEEP (Desktop Only - `hidden md:flex`)** | تظل مقتصرة على الشاشات الكبيرة والمكتبية. |
| **إجراء تسجيل الخروج** | أزرار متعددة متباينة في الترويسة وبطاقة المستخدم والسايدبار | **MERGE (Unified Action)** | توحيد الإجراء في الشريط العلوي للجوال وفي رأس صفحة الإعدادات/الحساب مع تأكيد احترازي موحد. |

---

## 2. ملخص توحيد البنية (Architecture Consolidation)

1. **تسجيل الدخول القياسي (Canonical Login)**:
   - مسار وحيد: `LoginView.jsx` نقي، سريع، responsive بالكامل، بدون أي روابط تنزيل خارجية أو تبويبات مربكة.
2. **الإعدادات القياسية (Canonical Settings)**:
   - مسار وحيد: `currentTab === 'settings'` عبر `SettingsView.jsx`.
   - أي نقرة على أيقونة الترس (في الشريط العلوي، في صفحة البيع، أو في لوحة التحكم) تؤدي حصراً إلى `handleNavigate('settings')`.
3. **لوحة التحكم القياسية (Canonical Dashboard)**:
   - مسار وحيد: `currentTab === 'home'` عبر `MobileHomeHub.jsx`.
4. **نقطة البيع القياسية (Canonical POS)**:
   - مسار وحيد: `currentTab === 'sale'` عبر `SaleScreen.jsx`.
